let score = 0
let gsize = 20



let snake = somwhere in the middle x3
    



let apple = somewhere on grid


let gameOver = false

listen for "key"
    if (key == 'ArrowUp') 
    if (key == 'ArrowDown') 
    if (key == 'ArrowLeft') 
    if (key == 'ArrowRight) 


function main() 
    setTimeout(function onTick()
        80)


function clearCanvas()
    fillStyle = "green"
    fillRect = (width, height)


function drawsnake() 
    snake fill = "blue"
    each grid of snake.
        fill rect with snake

function drawApple() 
    fillStyle = "red"
    fillRect(width, height)


function generateApple() 
    let validPosition = false
    while (!validPosition)
        apple in random spot on grid
        
        
        validPosition = not snake


function movesnake()
    const head = (0+x , 0+y)
    
    
    if (head hits wall)
        gameOver = true
        alert("Game Over. Score: " + score)
        reload
        return
    
    
    
    if (snake hits self) 
        gameOver = true
        alert("Game Over. Score: " + score)
        reload
        return
    
    
    snake.unshift(head)
    
   
    if (head eats apple) [
        score+
        generateApple()
        
      
        const totalGridSquares = (canvas.width / gsize) * (canvas.height / gsize)
        if (snake lentgh = grid) 
            gameOver = true
            ("Victory. Score: " + score)
            reload
            return
    ]
      else {
        snake = dead
    }


listen for "click",
    if (!gameOver) 
        